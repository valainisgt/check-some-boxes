{
  "private": true,
  "scripts": {
    "build-elm": "elm make src/Main.elm --output=src/elm.js",
    "test": "npm run build-elm && ava",
    "dev": "npm run build-elm && parcel src/index.html",
    "build": "npm run build-elm && parcel build src/index.html src/404.html --public-url /check-some-boxes"
  },
  "devDependencies": {
    "ava": "^2.4.0",
    "elm": "^0.19.0-no-deps",
    "parcel-bundler": "^1.12.3",
    "taiko": "^1.0.0"
  }
}
